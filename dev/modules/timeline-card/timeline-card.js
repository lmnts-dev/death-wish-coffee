/**
 * Initializes the javascript for the timeline-card module
 * @param {Object} el - The DOM Node containing the data-module="timeline-card" attribute.
 */

const timelineCard = (el) => {
  console.warn('initializing timelineCard module')
}

export default timelineCard
