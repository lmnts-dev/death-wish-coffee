/**
 * Initializes the javascript for the mirror-image-hero module
 * @param {Object} el - The DOM Node containing the data-module="mirror-image-hero" attribute.
 */

const mirrorImageHero = (el) => {
  console.warn('initializing mirrorImageHero module')
}

export default mirrorImageHero
