/**
 * Initializes the javascript for the cta-banner module
 * @param {Object} el - The DOM Node containing the data-module="cta-banner" attribute.
 */

const ctaBanner = (el) => {
  console.warn('initializing customerInfo module')
}

export default ctaBanner
