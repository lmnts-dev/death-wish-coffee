/**
 * Initializes the javascript for the post-header module
 * @param {Object} el - The DOM Node containing the data-module="post-header" attribute.
 */

const postHeader = (el) => {
  console.warn('initializing postHeader module')
}

export default postHeader
