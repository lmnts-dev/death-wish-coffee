/**
 * Initializes the javascript for the icon-title module
 * @param {Object} el - The DOM Node containing the data-module="icon-title" attribute.
 */

const iconTitle = (el) => {
  console.warn('initializing iconTitle module')
}

export default iconTitle
