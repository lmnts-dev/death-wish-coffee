/**
 * Initializes the javascript for the og-offer module
 * @param {Object} el - The DOM Node containing the data-module="og-offer" attribute.
 */

const ogOffer = (el) => {
  console.warn('initializing og-offer module')
}

export default ogOffer
