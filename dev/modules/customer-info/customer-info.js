/**
 * Initializes the javascript for the customer-info module
 * @param {Object} el - The DOM Node containing the data-module="customer-info" attribute.
 */

const customerInfo = (el) => {
  console.warn('initializing customerInfo module')
}

export default customerInfo
