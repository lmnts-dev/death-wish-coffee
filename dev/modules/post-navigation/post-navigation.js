/**
 * Initializes the javascript for the post-navigation module
 * @param {Object} el - The DOM Node containing the data-module="post-navigation" attribute.
 */

const postNavigation = (el) => {
  console.warn('initializing postNavigation module')
}

export default postNavigation
