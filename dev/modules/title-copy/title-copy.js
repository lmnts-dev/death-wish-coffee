/**
 * Initializes the javascript for the title-copy module
 * @param {Object} el - The DOM Node containing the data-module="title-copy" attribute.
 */

const titleCopy = (el) => {
  console.warn('initializing titleCopy module')
}

export default titleCopy
