/**
 * Initializes the javascript for the bolt-header module
 * @param {Object} el - The DOM Node containing the data-module="bolt-header" attribute.
 */

const boltHeader = (el) => {
  console.warn('initializing boltHeader module')
}

export default boltHeader
