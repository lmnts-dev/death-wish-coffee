/**
 * Initializes the javascript for the pdp-hero module
 * @param {Object} el - The DOM Node containing the data-module="pdp-hero" attribute.
 */

const pdpHero = (el) => {
  console.warn('initializing pdpHero module')
}

export default pdpHero
