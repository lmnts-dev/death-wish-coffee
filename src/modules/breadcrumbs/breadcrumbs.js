/**
 * Initializes the javascript for the breadcrumbs module
 * @param {Object} el - The DOM Node containing the data-module="breadcrumbs" attribute.
 */

const breadcrumbs = (el) => {
  console.warn('initializing breadcrumbs module')
}

export default breadcrumbs
