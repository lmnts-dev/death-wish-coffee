/**
 * Initializes the javascript for the title-description-card module
 * @param {Object} el - The DOM Node containing the data-module="title-description-card" attribute.
 */

const titleDescriptionCard = (el) => {
  console.warn('initializing titleDescriptionCard module')
}

export default titleDescriptionCard
