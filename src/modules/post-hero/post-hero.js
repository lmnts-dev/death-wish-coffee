/**
 * Initializes the javascript for the post-hero module
 * @param {Object} el - The DOM Node containing the data-module="post-hero" attribute.
 */

const postHero = (el) => {
  console.warn('initializing postHero module')
}

export default postHero
