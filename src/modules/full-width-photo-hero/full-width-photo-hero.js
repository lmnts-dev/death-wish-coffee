/**
 * Initializes the javascript for the full-width-photo-hero module
 * @param {Object} el - The DOM Node containing the data-module="full-width-photo-hero" attribute.
 */

const fullWidthPhotoHero = (el) => {
  console.warn('initializing fullWidthPhotoHero module')
}

export default fullWidthPhotoHero
