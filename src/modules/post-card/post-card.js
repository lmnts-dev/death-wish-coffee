/**
 * Initializes the javascript for the post-card module
 * @param {Object} el - The DOM Node containing the data-module="post-card" attribute.
 */

const postCard = (el) => {
  console.warn('initializing postCard module')
}

export default postCard
