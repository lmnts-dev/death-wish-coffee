/**
 * Initializes the site's styleguide module.
 * @constructor
 * @param {Object} el - The site's styleguide container element.
 */
const styleguide = el => {
  // console.log(el)
}

export default styleguide
