/**
 * Initializes the javascript for the section-cta module
 * @param {Object} el - The DOM Node containing the data-module="section-cta" attribute.
 */

const sectionCta = (el) => {
  console.warn('initializing sectionCta module')
}

export default sectionCta
