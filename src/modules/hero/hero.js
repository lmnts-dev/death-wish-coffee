/**
 * Initializes the javascript for the hero module
 * @param {Object} el - The DOM Node containing the data-module="hero" attribute.
 */

const hero = (el) => {
  console.warn('initializing hero module')
}

export default hero
