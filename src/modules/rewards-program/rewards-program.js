/**
 * Initializes the javascript for the rewards-program module
 * @param {Object} el - The DOM Node containing the data-module="rewards-program" attribute.
 */

const rewardsProgram = (el) => {
  console.warn('initializing rewardsProgram module')
}

export default rewardsProgram
