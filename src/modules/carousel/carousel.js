/**
 * Initializes the javascript for the carousel module
 * @param {Object} el - The DOM Node containing the data-module="carousel" attribute.
 */

const carousel = (el) => {
  console.warn('initializing carousel module')
}

export default carousel
