/**
 * Initializes the javascript for the customer-addresses module
 * @param {Object} el - The DOM Node containing the data-module="customer-addresses" attribute.
 */

const customerAddresses = (el) => {
  console.warn('initializing customerAddresses module')
}

export default customerAddresses
