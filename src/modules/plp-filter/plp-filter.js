/**
 * Initializes the javascript for the plp-filter module
 * @param {Object} el - The DOM Node containing the data-module="plp-filter" attribute.
 */

const plpFilter = (el) => {
  console.warn('initializing plpFilter module')
}

export default plpFilter
