/**
 * Initializes the javascript for the bolt-illustration-hero module
 * @param {Object} el - The DOM Node containing the data-module="bolt-illustration-hero" attribute.
 */

const boltIllustrationHero = (el) => {
  const breakpointLGMax = window.matchMedia('(max-width: 991px)')
  console.log(breakpointLGMax)
}

export default boltIllustrationHero
