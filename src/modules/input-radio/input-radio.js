/**
 * Initializes the javascript for the input-radio module
 * @param {Object} el - The DOM Node containing the data-module="input-radio" attribute.
 */

const inputRadio = (el) => {
  console.warn('initializing inputRadio module')
}

export default inputRadio
