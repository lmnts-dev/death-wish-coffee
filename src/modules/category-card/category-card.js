/**
 * Initializes the javascript for the category-card module
 * @param {Object} el - The DOM Node containing the data-module="category-card" attribute.
 */

const categoryCard = (el) => {
  console.warn('initializing categoryCard module')
}

export default categoryCard
