{%- assign custom_attrs = custom_attrs | default: false -%}
<div is="rewards-hero" inline-template{% if custom_attrs %} {{ custom_attrs }}{% endif %}>
  <section class="rewards-hero">
    <p>First name: ${firstName}</p> 
    <p>VIP Tier: ${tier ? tier : 'None'}</p>
    <p>Points Balance: ${pointsBalance}</p>
  </section>
</div>
