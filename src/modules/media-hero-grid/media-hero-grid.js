/**
 * Initializes the javascript for the media-hero-grid module
 * @param {Object} el - The DOM Node containing the data-module="media-hero-grid" attribute.
 */

const mediaHeroGrid = (el) => {
  console.warn('initializing mediaHeroGrid module')
}

export default mediaHeroGrid
