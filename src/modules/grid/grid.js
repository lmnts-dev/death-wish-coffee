/**
 * Initializes the javascript for the grid module
 * @param {Object} el - The DOM Node containing the data-module="grid" attribute.
 */

const grid = (el) => {
  console.warn('initializing grid module')
}

export default grid
