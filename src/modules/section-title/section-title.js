/**
 * Initializes the javascript for the section-title module
 * @param {Object} el - The DOM Node containing the data-module="section-title" attribute.
 */

const sectionTitle = (el) => {
  console.warn('initializing sectionTitle module')
}

export default sectionTitle
