/**
 * Initializes the site's image module.
 * @constructor
 * @param {Object} el - The site's image container element.
 */

const image = el => {
  console.warn('image module initialized')
}

export default image
